#include<stdio.h>
#include<stdio.h>




int main()
{
    int* ptr = (int*)malloc(sizeof(int));
    if(Ptr==NULL)
    {
        print("Memory not alloted ")
    }
    else 
    {
        *ptr  = 5;
    
         printf("value at ptr  = %d",*ptr);

    }
    
        //free(ptr);
    // free(ptr);
    // printf("%d",*ptr);


    return 0;
}